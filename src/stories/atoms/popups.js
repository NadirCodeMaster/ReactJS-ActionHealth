import React, { Fragment } from "react";
import Popup from "components/ui/Popup";
import * as demoContent from "../_support/demoContent";
import * as demoStyles from "../_support/demoStyles";

// @TODO Update to use hgToast

// export default {
//   title: 'Atoms/Popup',
//   argTypes: {
//     popupText: {
//       description:
//         'This option is only for the styleguide demo. This option allows customized text to see how it fills the popup',
//       control: {
//         type: 'text',
//       },
//     },
//   },
// };

// const defaultPopupText = demoContent.demoSentence;

// export const Success = (args) => {
//   const { popupText } = args;

//   return (
//     <Fragment>
//       <div style={demoStyles.disclaimerBlock}>{disclaimer}</div>
//       <Popup onClose={() => {}} message={popupText} severity={'success'} />
//     </Fragment>
//   );
// };

// Success.args = {
//   popupText: defaultPopupText,
// };

// export const Info = (args) => {
//   const { popupText } = args;

//   return (
//     <Fragment>
//       <div style={demoStyles.disclaimerBlock}>{disclaimer}</div>
//       <Popup onClose={() => {}} message={popupText} severity={'info'} />
//     </Fragment>
//   );
// };

// Info.args = {
//   popupText: defaultPopupText,
// };

// export const Error = (args) => {
//   const { popupText } = args;

//   return (
//     <Fragment>
//       <div style={demoStyles.disclaimerBlock}>{disclaimer}</div>
//       <Popup onClose={() => {}} message={popupText} severity={'error'} />
//     </Fragment>
//   );
// };

// Error.args = {
//   popupText: defaultPopupText,
// };

// export const Warning = (args) => {
//   const { popupText } = args;

//   return (
//     <Fragment>
//       <div style={demoStyles.disclaimerBlock}>{disclaimer}</div>
//       <Popup onClose={() => {}} message={popupText} severity={'warning'} />
//     </Fragment>
//   );
// };

// Warning.args = {
//   popupText: defaultPopupText,
// };

// const disclaimer = (
//   <React.Fragment>
//     <p>Popup will disappear after 6 seconds</p>
//   </React.Fragment>
// );
