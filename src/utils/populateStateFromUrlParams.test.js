// @TODO Need test coverage. This utility takes a component
// instance and calls its setState() method, and I'm not sure
// how to test that yet.

test('This test file has nothing useful in it yet', () => {
  let amiright = true;
  expect(amiright).toBe(true);
});
