// @TODO Need test coverage. This utility executes history.push(),
// and I'm not yet sure how to test that.

test('This test file has nothing useful in it yet', () => {
  let amiright = true;
  expect(amiright).toBe(true);
});
