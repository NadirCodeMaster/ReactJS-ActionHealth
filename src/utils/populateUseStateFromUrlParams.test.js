// @TODO Need test coverage. This utility sets state variables with
// a react component hook.  Need to research how to properly test this.

test('This test file has nothing useful in it yet', () => {
  let amiright = true;
  expect(amiright).toBe(true);
});
